[package]
name = "template_engine_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
wasm-bindgen = "0.2"
template_engine = { path = "../template_engine" }
