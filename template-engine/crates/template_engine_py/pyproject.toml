[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "template-engine"
version = "0.1.0"
description = "Python bindings for template_engine"
requires-python = ">=3.9"
